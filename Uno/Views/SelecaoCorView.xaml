<Window x:Class="Uno.Views.SelecaoCorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escolhe uma cor" Height="300" Width="350" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <StackPanel Margin="20" HorizontalAlignment="Center">
        <TextBlock Text="Escolhe uma cor" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Text="Seleciona a cor para continuar o jogo" Foreground="black" HorizontalAlignment="Center"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Background="red" Content="Vermelho" Click="BotaoCor_Click" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="0" Grid.Column="0"/>
            <Button Background="blue" Content="Azul" Click="BotaoCor_Click" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="0" Grid.Column="1"/>
            <Button Background="green" Content="Verde" Click="BotaoCor_Click" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="1" Grid.Column="0"/>
            <Button Background="yellow" Content="Amarelo" Click="BotaoCor_Click" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="1" Grid.Column="1"/>
        </Grid>
    </StackPanel>
</Window>