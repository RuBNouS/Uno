<Window x:Class="UnoDesktopGame.Views.SelecaoCorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escolhe uma cor" Height="300" Width="350" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <StackPanel Margin="20" HorizontalAlignment="Center">
        <TextBlock Text="Escolhe uma cor" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Text="Seleciona a cor para continuar o jogo" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,20"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Background="#E63946" Content="Vermelho" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="0" Grid.Column="0"/>
            <Button Background="#457B9D" Content="Azul" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="0" Grid.Column="1"/>
            <Button Background="#2A9D8F" Content="Verde" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="1" Grid.Column="0"/>
            <Button Background="#E9C46A" Content="Amarelo" Foreground="White" Width="120" Height="60" Margin="5" Grid.Row="1" Grid.Column="1"/>
        </Grid>
    </StackPanel>
</Window>