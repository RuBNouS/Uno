<UserControl x:Class="Uno.Views.ResultadosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="white">

    <Grid>
        <Border Background="White" Width="450" VerticalAlignment="Center" HorizontalAlignment="Center" 
                CornerRadius="15" Padding="30" Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="20" ShadowDepth="5"/>
            </Border.Effect>

            <StackPanel>
                <Border Background="#FFF3E0" CornerRadius="10" HorizontalAlignment="Center" Padding="15,5" Margin="0,0,0,15">
                    <TextBlock Text="Fim de jogo" Foreground="#E68A00" FontWeight="Bold" FontSize="12"/>
                </Border>

                <TextBlock Text="{Binding NomeVencedor, StringFormat='{}{0} venceu!'}" 
                           FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#333333"/>

                <TextBlock Text="Partida concluída" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,30"/>

                <TextBlock Text="Classificação" Foreground="Gray" FontSize="14" Margin="0,0,0,10"/>

                <ItemsControl ItemsSource="{Binding Ranking}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding CorFundo}" CornerRadius="8" Padding="15,10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Posicao}" Grid.Column="0" VerticalAlignment="Center" Foreground="Gray" FontSize="14"/>

                                    <Border Grid.Column="1" Width="32" Height="32" CornerRadius="16" Background="{Binding CorFundoAvatar}" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Iniciais}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{Binding CorTextoAvatar}" FontWeight="Bold" FontSize="12"/>
                                    </Border>

                                    <TextBlock Text="{Binding Nome}" Grid.Column="2" VerticalAlignment="Center" Foreground="#333333" FontSize="16" FontWeight="SemiBold"/>

                                    <TextBlock Text="{Binding Pontos, StringFormat='{}{0} pts'}" Grid.Column="3" VerticalAlignment="Center" Foreground="#333333" FontSize="14" FontWeight="SemiBold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid Margin="0,30,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Command="{Binding ProximaPartidaCommand}" Content="Nova partida" Background="#E63946" Foreground="White" Height="45" Margin="0,0,10,0" FontSize="16" FontWeight="SemiBold" Style="{StaticResource RoundedButtonStyle}"/>
                    <Button Grid.Column="1" Command="{Binding SairCommand}" Content="Sair" Background="White" Foreground="#333333" BorderBrush="#DDDDDD" BorderThickness="1" Height="45" FontSize="16" Style="{StaticResource RoundedButtonStyle}"/>
                </Grid>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>